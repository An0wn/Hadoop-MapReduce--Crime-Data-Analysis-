RegionCrimeCount.java has program for region definition with first three digits.
RegionCrimeCount_1.java has program for region definition with first digit.
error.java has program that generates error.

I have used the command: conf.setNumReduceTasks(7) to manually set number of reducers which is commented in all the .java files.

